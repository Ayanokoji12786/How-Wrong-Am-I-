import { useState } from 'react'

type User = { id: string; email: string; username: string; displayName: string }
export function AuthModal({ onClose, onAuthenticated }: { onClose: () => void; onAuthenticated: (user: User) => void }) {
  const [mode,setMode]=useState<'login'|'signup'>('signup'),[error,setError]=useState(''),[busy,setBusy]=useState(false)
  const submit=async(event:React.FormEvent<HTMLFormElement>)=>{event.preventDefault();setBusy(true);setError('');const body=Object.fromEntries(new FormData(event.currentTarget));const response=await fetch(`/api/auth/${mode}`,{method:'POST',credentials:'include',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});const data=await response.json() as {user?:User;error?:string};setBusy(false);if(!response.ok||!data.user)return setError(data.error??'Could not authenticate.');onAuthenticated(data.user)}
  return <div className="modal-backdrop"><form className="modal forecast-form" onSubmit={submit}><button className="close-button" type="button" onClick={onClose}>×</button><p className="eyebrow">{mode==='signup'?'START YOUR PROFILE':'WELCOME BACK'}</p><h2>{mode==='signup'?'Meet your future self.':'Pick up where reality left off.'}</h2><p className="modal-lead">Your forecasts are private by default and stored in your personal calibration journal.</p>{mode==='signup'&&<><label className="field-label">Display name<input name="displayName" required minLength={2}/></label><label className="field-label">Username<input name="username" required minLength={3} pattern="[A-Za-z0-9_]+"/></label></>}<label className="field-label">Email<input name="email" type="email" required/></label><label className="field-label">Password<input name="password" type="password" required minLength={8}/></label>{error&&<p className="form-error">{error}</p>}<button className="primary-button lock-button" disabled={busy}>{busy?'One moment…':mode==='signup'?'Create my profile →':'Sign in →'}</button><p className="lock-note">{mode==='signup'?'Already have a profile?':'New here?'} <button className="inline-link" type="button" onClick={()=>setMode(mode==='signup'?'login':'signup')}>{mode==='signup'?'Sign in':'Create one'}</button></p></form></div>
}
